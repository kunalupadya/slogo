package Parser;

import java.util.regex.*;

/*
For predefined commands, should I just hard code the command?
And for user defined commands, how are we gonna store them? in xml format or just in a text file
*/

public class ParseCommand {

    private HandleError = new HandleError();

    public ParseCommand(String input){

    }
}
